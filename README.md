# Elevvo Internship - Task 5

## Data Requirements
- **Dataset:** MovieLens 100K (`ml-100k` folder)
- **Expected local path:** `../../ml-100k` from this task directory.
- **Common required files:** `u1.base`, `u1.test`, `u.item` (plus supporting MovieLens files).

## Repository Data Policy
- Prefer not committing full raw MovieLens folder into each task repo copy.
- Keep one local dataset copy and document the path.
- Include dataset source link in project notes/readme if publishing.

## Quick Setup
1. Download MovieLens 100K and extract to `Machine Learning Track/ml-100k`.
2. Confirm files like `u1.base`, `u1.test`, and `u.item` are present.
3. Run `Movie_Recommendation_System.ipynb`.
